#spring:
#  datasource:
#    username: devv
#    password: manchester
#    url: jdbc:mysql://localhost:3306/medispatch
spring:
  jpa:
    hibernate:
      ddl-auto: none

logging:
  level:
    org:
      springframework:
        jdbc:
          datasource:
            init:
              ScriptUtils: debug
